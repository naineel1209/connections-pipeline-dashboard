# Anti Slop Follow-up 003

Date: 2026-09-05  
Mode: AFTER  
Scope: Personal greetings and outreach message situations

## Approved Repairs

The approved findings 1 and 2 are complete.

1. `renderMessageTemplate` now replaces `{firstName}` with the first word in the stored contact name.
   `{name}` remains a first-name alias for existing templates.
   A connection named `Abhishek Kumar` now produces `Hi Abhishek,`.

2. Profile settings now store two templates in `profiles.message_template`.
   The template selector provides `Default outreach` and `Applied outreach`.
   The message drawer provides the same selector.
   It starts on `Applied outreach` for openings marked as applied on the portal.
   Kanban quick copy uses the same applied-state selection.

Existing plain-text templates remain the `Default outreach` template.
Missing or malformed structured templates use the default templates.

## Decision Reasons

- The selector stays in the message drawer because it changes the current message task.
- Profile settings keep template editing because templates are profile-level preferences.
- First-name greetings use the stored contact name and need no new data field.
- Standard buttons support keyboard and touch input.
- The two-column selector keeps both situations visible in the compact operational interface.

## Verification

- `npm run lint` passed.
- `npm test` passed with 13 tests.
- `npm run build` passed.
- The tests cover first-name rendering, legacy text migration, and structured template round trips.
- The selector buttons use native buttons, `aria-pressed`, and 44px minimum height.
- Authenticated browser checks need a Supabase user session. This environment has no supplied session.

## Delivery Gate

### Hard Gate: PASS

R-26 passes. Both message actions work through real buttons and persisted templates.

### Purpose-Gate: PASS

The focused drawer and profile settings match `DESIGN.md`.

### Liveliness: PASS

The change keeps ENERGY 2, RHYTHM 2, and MOTION 1.

### Craftsmanship and Quality Locks: PASS WITH LIMIT

Automated checks pass. Source review confirms keyboard controls and mobile tap targets.
Authenticated browser interaction remains unverified without a Supabase user session.
